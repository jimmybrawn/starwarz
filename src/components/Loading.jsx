import React from "react";

import "../style/Loading.css";
const Loading = function({ title, date, url, characters }) {
  return <div className="Loading">⏱ Loading...</div>;
};

export default Loading;
